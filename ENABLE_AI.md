# 🤖 تفعيل الذكاء الاصطناعي - Enable AI Models

## المشكلة الحالية ⚠️

النظام الآن يعمل في **وضع القواعد (Rule-Based Mode)** بدلاً من استخدام نماذج الذكاء الاصطناعي.

السبب: **مكتبات TensorFlow غير مثبتة**

## ✅ الحل السريع

### 1. تثبيت TensorFlow

افتح PowerShell في مجلد المشروع ونفذ:

```powershell
cd backend
.\venv\Scripts\Activate.ps1
pip install tensorflow==2.15.0
```

 أو إذا واجهت مشكلة في الحجم:

```powershell
pip install tensorflow-cpu==2.15.0
```

### 2. إعادة تشغيل الـ Backend

```powershell
# أوقف الـ Backend الحالي (Ctrl+C)
# ثم شغله من جديد
.\start-backend.ps1
```

### 3. التحقق من التفعيل

بعد إعادة التشغيل، يجب أن ترى في الـ logs:

```
✓ Injury detection model loaded from ...
```

بدلاً من:

```
⚠ Model not found - using rule-based detection
```

---

## 📊 حالة الـ AI الحالية

| المكون | الحالة | الوضع الحالي |
|--------|--------|---------------|
| **Injury Detection** | ⚠️ معطل | Rule-based (لون أحمر = نزيف) |
| **Severity Scorer** | ✅ يعمل | قواعد حسابية بسيطة |
| **Medical Chatbot** | ⚠️ معطل | ردود مبرمجة مسبقاً |

---

## 🔍 ماذا يحدث بدون AI؟

### بدون TensorFlow:
1. **كشف الإصابات**: يستخدم قواعد لونية بسيطة
   - كل ما هو أحمر = نزيف محتمل
   - كل ما هو أزرق/أسود = كدمة محتملة
   - **دقة منخفضة**

### مع TensorFlow (بعد التثبيت):
1. **كشف الإصابات**: نموذج AI مدرب على 7 أنواع
   - bleeding (نزيف)
   - bruise (كدمة)
   - burn (حرق)
   - cut (جرح)
   - fracture (كسر)
   - swelling (تورم)
   - **دقة أعلى بكثير**

---

## 🎯 نماذج AI المتوفرة

### ✅ متوفرة حالياً:
- `injury_detector.tflite` (228 KB) - كشف أنواع الإصابات
- `injury_detector.h5` (493 KB) - نفس النموذج بصيغة Keras

### ⚠️ تحتاج تفعيل:
- **Medical Chatbot** (يحتاج Ollama + MedAlpaca model)

---

## 💡 خيارات إضافية

### إذا أردت تفعيل الـ Chatbot الطبي:

1. تثبيت Ollama:
```powershell
# حمّل من https://ollama.ai
```

2. تحميل نموذج طبي:
```powershell
ollama pull medalpaca
```

3. إعادة تشغيل Backend

---

## 🚀 الأوامر السريعة

### التثبيت الكامل:
```powershell
cd backend
.\venv\Scripts\Activate.ps1

# TensorFlow للـ AI
pip install tensorflow==2.15.0

# NumPy (مطلوب)
pip install numpy==1.24.3

# OpenCV للصور
pip install opencv-python==4.8.1.78

# PIL للصور
pip install Pillow==10.1.0
```

### التحقق:
```powershell
python
>>> import tensorflow as tf
>>> print(tf.__version__)
2.15.0
>>> exit()
```

---

## 📝 ملاحظات مهمة

1. **الحجم**: TensorFlow حوالي 400-500 MB
2. **البديل الخفيف**: استخدم `tensorflow-cpu` إذا لا تحتاج GPU
3. **النموذج موجود**: الـ AI model نفسه موجود في `backend/ai_engine/models/`
4. **المشكلة فقط**: نقص مكتبة TensorFlow Lite للتشغيل

---

## ✅ بعد التثبيت

ستلاحظ الفرق:
- ✨ كشف دقيق للإصابات من الصور
- 🎯 تصنيف أنواع الإصابات (نزيف، حرق، كسر، إلخ)
- 📊 نسبة ثقة لكل تشخيص (confidence score)
- ⚡ تحليل أسرع وأوضح

---

**هل تريد تثبيت TensorFlow الآن؟** 🤔
